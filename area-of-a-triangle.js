// challenge 5

function calcularAreaTriangulo(b, a) {
    return (b * a) / 2;
}

let base = 7, altura = 5;
let area = calcularAreaTriangulo(base, altura);

console.log(`A área do triângulo entre ${base} e ${altura} é de: ${area}`);

